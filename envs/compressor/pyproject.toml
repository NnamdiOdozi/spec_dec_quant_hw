[project]
name = "hw3-compressor-env"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "llmcompressor==0.12.0",
  "ipykernel",
  "transformers",
  "accelerate",
]

[tool.uv]
package = false
